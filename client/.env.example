# ============================================================
# SEAORA — La Perle (frontend)
# Copy this file to `.env` inside client/ and fill it in.
# ============================================================

# Absolute origin of the backend API. Required whenever the client is
# deployed separately from the server (e.g. client on Netlify, API on a VPS).
VITE_API_URL=https://api.yourdomain.com
